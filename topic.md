# 주제 (M5-01)

- **주제 한 문장**: 내년에 대학교 1학년으로 입학할 예정인 나는, 초고령사회(65세 이상 인구 20.33%)와 낮은 실질금리(1.034%) 속에서 취업·결혼·출산 등 인생 이벤트를 반영했을 때 목표 나이에 자산이 얼마나 남을지 계산하지 못한다.
- **대상 한 사람**: 나 자신(페르소나 타노스) — 27세, 내년 대학교 1학년 입학 예정, 현재 무직·소득 없음, 보유 재산 있음
- **이 주제가 나온 숫자(근거 번호)**: D1(65세 이상 인구 비중 20.33%), D2(실질금리 1.034%)
- **지금은 어떻게 해결하나(기존 대안)**: 없음 / 막연히 "나중에 생각하자" — 또는 엑셀로 직접 계산하려다 변수가 많아 포기
- **가장 위험한 가정 1개**: 물가상승률·금리·소득 변화가 앞으로도 과거 평균과 비슷한 범위에서 움직일 것이라는 가정 (실제로는 예측 불가능한 변수)
- **앱에 쓸 데이터**: `data/public/kor_macro_annual.js`(물가상승률·금리·65세 이상 비중, 2010~2025 실측 + 기본 가정치) + 4대보험료율·평균 결혼/양육비 근사치(`data_sources.md` D3~D5) + 사용자 직접 입력값 다수
- **오늘 미룰 것**: 연금저축·IRP 세액공제 계산, 세율의 연도별 자동 변경(법 개정 반영), 자산군별 감가상각을 개별 자산 단위로 세분화하는 것 — 이번 F-01은 "연 1회 스냅샷 시뮬레이션"으로 단순화하고 세부 확장은 다음 개선으로 남김

## 오늘 만들 한 화면 (F-01 방향 메모 — 8단계 요구사항에서 확정)

한 페이지 시뮬레이터. 빈칸에 값을 입력하면 목표 나이(N세) 시점의 예상 자산을 계산해 보여준다.

**입력값(사용자 직접 조정)**
1. 현재 상태: 입학 예정/재학/졸업·취준 (선택) + 대학 입학 나이(기본 28세, 조정 가능) + 취업까지 걸리는 기간(N년)
2. 초기 자산 (직접 입력, 만원 단위)
3. 취업 시 세전 소득 (직접 입력)
4. 원천징수 세율: 기본값은 D6(2025년 귀속 국세청 누진세율표 8구간) + D3(4대보험료율)를 그대로 적용하되, **모든 구간의 세율·요율을 사용자가 직접 덮어쓸 수 있는 입력칸** 제공 — 세금은 향후 증세/감세로 바뀔 수 있어 고정값으로 두지 않음
5. 결혼·출산 이벤트: 발생 여부·시점·소득 변화·지출 증가 (기본값은 D4·D5 근사치, 조정 가능)
6. 물가상승률·명목금리·실질금리 (기본값은 `kor_macro_annual.js` 최근값, 조정 가능)
7. 보유 자산 포트폴리오별 예상 수익률/감가상각률 (직접 입력)
8. 국민연금 예상 수령액 (직접 입력, 기본값 없음 — 사용자 설정)
9. 예상 수명 (직접 입력)
10. 4대보험료율 (D3 근사치를 기본값으로 자동 적용, 세전 소득에서 원천공제)

**기본값 원칙 (입력 부담 줄이기)**: 평균값이 있는 항목은 미리 채워 두고 사용자는 바꾸고 싶은 칸만 고친다. 페르소나의 거절 이유("입력할 게 너무 많아서 귀찮다")에 대응하는 설계다.
- 초혼 나이: D7(남 33.9세 · 여 31.6세) — 성별 선택에 따라 기본값 적용
- 세율·4대보험료율: D6·D3 그대로
- 결혼 비용: D4, 자녀 양육비: D5(월 72.1만 원, 18세까지) — 조사 시점·범위 차이는 화면에 표시
- 물가상승률·금리: `kor_macro_annual.js` 최근값
- 자녀 수 기본값 2명: 통계상 평균이 아니라 **기본 설정값**이므로 화면에 "평균"이라고 쓰지 않고 "기본 설정"으로 표시한다(출처 없음, 사용자가 바꾸는 값)
- 기본값이 채워져 있어도 어떤 값이 기본값이고 어떤 값을 내가 바꿨는지 화면에서 구분한다
- **소득세율**: 입력한 소득이 속한 D6 구간의 세율을 자동 적용한다(구간이 바뀌면 세율도 자동으로 바뀜). 사용자가 구간별 세율을 직접 덮어쓸 수도 있다
- **결혼·양육비 고비용 참고값**: 기본값(평균) 옆에 "고비용 참고(근사치)"를 작은 글씨로 함께 표시해 사용자가 직접 찾아보지 않아도 되게 한다. 다만 상위 10%의 정확한 통계는 찾지 못해 D8의 고비용 사례(지역 최고값·상위 20% 등)를 쓰며, 화면에는 "상위 10%"라고 쓰지 않고 "고비용 참고(근사치)"라고 표시한다

**제외 확정**: 연금저축·IRP 세액공제는 계산에서 제외

**결과**: 목표 나이(N세) 시점 예상 순자산, 연도별 자산 추이 그래프(가능하면)

이 메모는 7단계(MVP·RICE)와 8단계(요구사항) 작성 시 그대로 이어받아 R-01/R-02/R-03으로 구체화한다.
